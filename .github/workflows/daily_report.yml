name: Daily Stock Analysis Report

on:
  schedule:
    # 🕒 시간 설정 (UTC 기준)
    # 한국 시간 아침 9시 35분 = UTC 같은 날 0시 35분
    # 화~토요일 실행 (미국 장 마감 후 다음 날 아침 리포트)
    - cron: '26 0 * * 2-6'

  workflow_dispatch:

jobs:
  stock-analysis:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # [수정됨] google-api-core 추가 (에러 해결!)
        pip install pandas yfinance google-genai google-api-core sqlalchemy prefect python-dotenv tabulate markdown jinja2 psycopg2-binary

    - name: Run Main Script
      env:
        GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        EMAIL_USER: ${{ secrets.EMAIL_USER }}
        EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        EMAIL_RECEIVER: ${{ secrets.EMAIL_RECEIVER }}
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
      run: |
        python -m app.main
